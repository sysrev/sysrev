(ns sysrev.test.browser.json-viewer)

;; test that
;; json cursors reset when switching sources / projects
;; make sure there is a light and dark mode version of the viewer
;; make sure blank preview is not just {
;; tab should be disabled when there are no previews to view
