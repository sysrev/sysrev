-- these are indexes on primary keys, which should already have indexes created
-- automatically from the primary key constraint
drop index a_article_id_idx;
drop index ac_id_idx;
drop index c_id_idx;
drop index wu_id_idx;
